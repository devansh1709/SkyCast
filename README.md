# SkyCast
SkyCast is a full-stack weather dashboard built with Spring Boot and vanilla JavaScript that delivers current weather conditions and forecast data. It integrates with an external weather API, processes the data in the backend, and dynamically renders it through an interactive and responsive UI.
